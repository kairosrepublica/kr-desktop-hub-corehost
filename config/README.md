# Configuration

`config.example.json` is sanitized and safe to commit.

Do not commit real local configuration.

The Owner's runtime configuration will be created outside the public repository or as an ignored local file.

The public example intentionally uses a generic data root:

```text
%LOCALAPPDATA%\KR\KRDesktopHub\
```

Owner-specific storage overrides remain local and untracked.
